import { Box, Typography } from '@mui/material';
import React from 'react';

const TrackingDashboard: React.FC = () => (
  <Box p={3}>
    <Typography variant="h5">Theo dõi vận chuyển</Typography>
  </Box>
);

export default TrackingDashboard;
