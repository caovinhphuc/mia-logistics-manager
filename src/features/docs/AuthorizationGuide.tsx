import { Box, Typography } from '@mui/material';
import React from 'react';

const AuthorizationGuide: React.FC = () => (
  <Box p={3}>
    <Typography variant="h5">Hướng dẫn phân quyền</Typography>
  </Box>
);

export default AuthorizationGuide;
