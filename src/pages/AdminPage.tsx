import { Box, Typography } from '@mui/material';
import React from 'react';

const AdminPage: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4">Admin</Typography>
    <Typography>Trang quản trị hệ thống</Typography>
  </Box>
);

export default AdminPage;
